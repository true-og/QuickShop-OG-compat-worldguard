create:
  default-allow: false
trade:
  # False blocks every shop in a region that does not carry the flag.
  default-allow: false
# Amount limit for per region, -1 for no limit.
max-shops-in-region: -1
